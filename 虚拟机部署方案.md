# 快手金币脚本虚拟机部署架构与实施指南

本文档旨在指导如何将快手刷金币脚本部署在虚拟机中，并通过本地浏览器进行配置和管理，支持多账号及多手机并发。

---

## 1. 架构总览

*   **部署环境**：虚拟机（推荐 Ubuntu Server 或 Windows，为方便 USB 直通管理，建议 Windows 10/11 虚拟机可能对驱动支持更友好，但 Linux 资源占用更低）。
*   **物理设备**：
    *   宿主机（Windows PC）：用于运行虚拟机、浏览器访问控制台。
    *   多部手机：通过 USB 扩展坞连接到宿主机，并直通给虚拟机。
*   **软件栈**：
    *   **签名服务**：`ksapi.py` (Python + Flask + Frida) —— 负责与手机通信生成签名。
    *   **控制中台**：`web-server.js` (Node.js + Express) —— 负责网页交互、任务调度、日志管理。
    *   **业务脚本**：`ks开源本.js` (Node.js) —— 核心刷金币逻辑。
*   **网络流向**：
    *   宿主机浏览器 -> 虚拟机 IP:端口 (配置/控制) -> 虚拟机内 Node 服务。
    *   虚拟机内 Node 服务 -> 虚拟机内 Python 签名服务 -> USB 直通 -> 手机 App。
    *   虚拟机内 Node 服务 -> 互联网 -> 快手官方 API。

---

## 2. 阶段性实施计划

### 第一阶段：虚拟机环境准备 (预计 2-3 小时)

**目标**：虚拟机能识别所有 USB 手机，且基础开发环境就绪。

1.  **虚拟机创建**：
    *   安装 VMware Workstation 或 VirtualBox。
    *   安装操作系统（建议 Ubuntu 22.04 LTS 或 Windows 10）。
    *   配置网络为 **桥接模式 (Bridged)**，确保虚拟机拥有独立局域网 IP，方便宿主机访问。
2.  **USB 直通配置**：
    *   将 USB 扩展坞插入宿主机。
    *   在虚拟机设置中，将识别到的所有手机设备 USB 端口直通给虚拟机。
    *   **验证**：在虚拟机内运行 `lsusb` (Linux) 或设备管理器 (Windows)，确认能看到所有手机。
3.  **环境安装**：
    *   安装 `Node.js` (LTS 版本) 和 `npm`。
    *   安装 `Python 3.x` 和 `pip`。
    *   安装 `adb` 工具包。
    *   **验证**：在虚拟机终端运行 `adb devices`，必须能列出所有连接的手机。

### 第二阶段：多手机签名服务部署 (预计 3-5 小时)

**目标**：所有手机都能响应签名请求，实现并发支持。

1.  **手机端准备**：
    *   每台手机开启 USB 调试。
    *   推送对应架构的 `frida-server` 到每台手机并启动（可编写批量脚本）。
    *   每台手机安装并运行快手极速版。
2.  **签名服务改造 (`ksapi.py`)**：
    *   **多设备支持**：原脚本可能默认只连第一台设备。需要修改 `ksapi.py`，使其能识别多个设备 ID。
    *   **接口映射**：建议通过 URL 参数指定设备，例如 `POST /sign?device=device_id_1`。
    *   **Frida 连接**：代码需遍历 `frida.get_device_manager().enumerate_devices()` 来动态挂载多台手机。
3.  **服务启动**：
    *   安装依赖：`pip install frida flask`。
    *   启动 Python 服务：`python ksapi.py`。
    *   **验证**：在虚拟机内用 `curl` 测试不同设备的签名接口是否正常返回。

### 第三阶段：控制中台与业务脚本集成 (预计 4-6 小时)

**目标**：通过网页配置参数，后台自动调度任务。

1.  **业务脚本改造 (`ks开源本.js`)**：
    *   **去卡密**：彻底清理原有卡密验证逻辑。
    *   **接口本地化**：将所有远程加密/签名 API 请求改为调用 `http://127.0.0.1:5000` (虚拟机本地的 Python 服务)。
    *   **多设备适配**：脚本运行时需知道当前任务对应哪台手机（用于请求对应的签名服务）。建议通过环境变量传入设备 ID。
2.  **Web 控制台开发 (`KSMoney` 项目)**：
    *   **目录结构**：
        ```text
        KSMoney/
        ├── public/           # 前端静态文件 (index.html, css, js)
        ├── scripts/          # 放置 ks开源本.js, ksapi.py 等
        ├── config/           # 存放账号配置 json
        ├── server.js         # Node.js 后端入口
        └── package.json
        ```
    *   **前端页面**：
        *   **账号管理**：支持添加/编辑/删除账号（Cookie、User-ID 等）。
        *   **设备绑定**：将账号与 USB 设备 ID 进行绑定（下拉选择当前在线设备）。
        *   **全局配置**：设置金币上限、运行轮数、任务类型。
        *   **控制面板**：一键启动/停止所有任务，实时查看简要日志。
    *   **后端逻辑 (`server.js`)**：
        *   提供 API 保存配置到 `config.json`。
        *   提供 API 获取当前 ADB 设备列表供前端选择。
        *   `/run` 接口：根据配置，为每个账号启动一个 `child_process` 运行 `ks开源本.js`，并注入对应的环境变量（如 `DEVICE_ID`）。
        *   日志实时推送（WebSocket 或简单的轮询）。

### 第四阶段：整合测试与交付 (预计 2 小时)

1.  **全链路测试**：
    *   在宿主机浏览器打开 `http://虚拟机IP:3000`。
    *   配置 2 个以上账号，绑定不同手机。
    *   点击运行，观察虚拟机内 CPU/内存占用，以及手机端是否正常执行。
2.  **开机自启**：
    *   配置虚拟机开机自动运行 `server.js` 和 `ksapi.py`。
3.  **文档交付**：
    *   编写简单的使用说明，记录如何新增手机、如何更新脚本。

---

## 3. 关键技术点提示

1.  **多设备 Frida 区分**：
    在 Python 中连接指定设备：
    ```python
    device = frida.get_device(id="设备序列号")
    session = device.attach("快手极速版")
    ```
    需要修改 `ksapi.py` 接收请求参数中的 `device_id` 来动态选择设备。

2.  **ADB 连接稳定性**：
    多手机连接时，USB 供电和线缆质量至关重要。若频繁掉线，请检查扩展坞是否带独立供电。

3.  **IP 地址固定**：
    建议在路由器中为虚拟机分配静态 IP，避免重启后 IP 变动导致宿主机浏览器无法访问。

## 4. 所需文件清单

*   `ks开源本.js` (已修改去卡密、本地化接口版)
*   `ksapi.py` (已修改支持多设备版)
*   `frida-server` (对应手机架构)
*   `server.js` (Web 控制台后端)
*   `public/index.html` (Web 控制台前端)
*   `package.json` & `requirements.txt`

此方案实现了**配置简单、运行透明、硬件隔离**的需求，非常适合长期稳定运行。
